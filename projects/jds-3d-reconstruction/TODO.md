# 执行 TODO：双表示互导优化的成立条件

> 任务 ID 由 `project_id + 初始任务文本` 确定；修改描述时保留 ID。完成必须有对应 `EXP-*` 或等价可复现证据。
> **现状唯一入口见 [`STATUS.md`](STATUS.md)**；立论与三次修正见 [`sources/2026-08-24_mutual_guidance_position.md`](sources/2026-08-24_mutual_guidance_position.md)（`IDEA-4DC79F20C0C6`）。

## P0 — 前置：可复现性与数据

| 状态 | ID | 优先级 | 行动 | 验收 |
|---|---|---:|---|---|
| [x] | `TASK-B24F6E8A11` | P0 | 取得 MGSR 代码并跑通 | **已完成**。DTU scan24 三阶段 geo/ref/total 全部完成，2h07m，三个点云目录齐备 |
| [-] | `TASK-7E31A0C5D9` | P0 | 取得 GS-Octree 代码并复现论文数值 | **受阻，且不可能直接达成**。`train-big.py` 依赖论文第一阶段产出的 `SDF_512_<scene>.npz`，公开仓未提供；连同另五处不完整，论文数字无法复现。**改为：所有对照组用我们自己的基线**，并向作者索取 `pretrain/` |
| [x] | `TASK-0D9C53BF27` | P0 | 下载数据集，固定评测协议 | **已完成，但数据集与原计划不同**。改用 DTU scan24（MGSR 支持且有官方真值），未用 NeRF-Synthetic / OmniObject3D。评测为 DTU 官方协议，实现于 `tools/dtu_eval.py`，含坐标系自检 |

## P1 — 证伪实验

**顺序已变更。** 原以 E-α 为最高优先级，但 MGSR 消融表 Model A/B/K/M 已覆盖
「有无互导」与「单向/双向」，故 E-α 降级为基线自检；核心实验改为 E-ζ。
理由见 [`sources/2026-08-24_mutual_guidance_position.md`](sources/2026-08-24_mutual_guidance_position.md) §11.5。

| 状态 | ID | 优先级 | 行动 | 检验 | 结果 |
|---|---|---:|---|---|---|
| [x] | `TASK-6C0F82D3E7` | P0 | 旋钮敏感性：对照 3 种子波动 | **H2** | **已完成且 H2 成立**。噪声地板 0.36%（ref）/0.38%（geo）；`w_depth` 从 0.01 到 0.5 使 ref 支劣化 5.4%（约 30 σ）、geo 支改善 0.91%（区间不重叠）。**效应量远超种子波动** |
| [x] | `TASK-3F92D07A5B` | P0 | E-ζ：扫描 geo→ref 通道权重，画渲染×几何前沿 | 耦合强度是否可调 | **首轮完成**（w ∈ {0.001, 0.01, 0.05, 0.5}）。发现**内部最优**而非单调权衡 |
| [ ] | `TASK-C81E4B60D9` | P0 | 内部最优点确认：`w=0.05` 在 seed 1/2 复现，补 `w=0.1` | 最优点是否真实 | **进行中**。预注册判据：三种子均优且幅度 ≥2% 则成立 |
| [ ] | `TASK-91BD7E2A45` | P0 | E-β′：固定 `w=0.5`，仅替换 depth 的 max 归一化为稳健尺度对齐 | **失效机制** | 待做。**若确认，贡献从「发现现象」升为「给出原因与修法」** |
| [ ] | `TASK-A5D3E17C42` | P1 | 跨场景验证：scan37 / 55 / 65 等重复 E-ζ | 单场景外是否成立 | 待做。**当前最大的未解除局限** |
| [ ] | `TASK-7B04FE9C81` | P1 | 扫描 `w2`（ref→geo，直接作用于几何的通道） | 另一条通道的效应 | 待做 |
| [ ] | `TASK-2E6A9D4F03` | P1 | 用 `extract_mesh_tsdf.py` 出 TSDF 网格重评 | 数字可与论文比较 | 待做。当前用高斯中心，**绝对值不可与论文比** |
| [-] | `TASK-4A8E1FC603` | P2 | 循环消融：A / B / A→B / A↔B 四路对比 | **H1** | **降级为基线自检**。MGSR Model A/B/K/M 已覆盖；我们可加的是**两种半双向配置**（单独删两个 `.detach()` 之一），MGSR 未测过 |
| [ ] | `TASK-E5730BA914` | P2 | 早期可预测性：寻找预示成败的早期可测信号 | **H3** | 待做。`ezeta_trace.tsv` 已按 50 步记录两条通道损失，可画 GEAR 式动力学曲线 |

## 待研究者执行

- **向 MGSR 作者索取** `pretrain/` 目录与 GS-Octree 第一阶段的确切命令；
  并核对论文正文与表格的模型编号是否互换（auto-stop 与 iterations 两段疑似对调）。
- **确认公开代码是否等于论文实验版本**——六处不完整使这一点存疑。
- **可用于谈合作的事实**：MGSR 引用了 GS-Octree，两篇有明确引用关系；
  两篇表面重建综述均把 GS-Octree 与 GSDF、NeuSG、3DGSR 同级列为双表示代表方法。

## 已完成的调研

- [x] 七个候选方向连续证伪，方法与教训见 [`sources/2026-08-23_survey_methodology.md`](sources/2026-08-23_survey_methodology.md)。
- [x] 会议全文正查工具链，语料 1239 篇；收敛性/稳定性分析主题级为 0。
- [x] **通道 B 引文图谱**（`TASK` 原列于「待研究者执行」，已由本项目完成）：
  1674 篇引用者 → 189 命中 → **188 篇全文**，104 篇五类探针全为 0，无一篇分析双表示互导的成立条件。
- [x] **检索盲区已补**：原记「未覆盖 SIGGRAPH / TOG / TVCG」，
  已由 arXiv cs.GR 通道与引文图覆盖（GS-ROR2 即 TOG 2024，已纳入）。
- [x] 专门综述全文核验：PeerJ CS 2025，正文 90269 字符，收敛性论述 0 / 失败条件 0 / 误差传播 0。

**残余缺口**（如实记录）：引文图种子中 2DGS(1000)、SuGaR(915) 触顶截断；
1674 篇中 524 篇无 arXiv id 取不到全文；
Expert Systems with Applications 2026 那篇综述因付费墙**未能核验**。

## 历史范围（驾驶场景，2026-08-23，已终止）

原方向为观测受限条件下的驾驶场景行人重建。终止原因：Hierarchy UGP (ICCV 2025) 已取得
human-region +8 dB；目视核验确认被丢弃的行人绝大多数确属信息不足，明确的模型失败仅 1/53。
相关产出（观测充分性分析、分层评测器、`gpu-budget` skill）保留复用。详见 `plans/CHANGELOG.md`。

## 归档：FastRelight 时期的任务 ID（2026-08-18，已终止）

保留于此仅为使历史 `EXP-*` 记录的 task 链接可解析。这些任务不再执行。

| 状态 | ID | 原行动 |
|---|---|---|
| [-] | `TASK-9A7AF40BC2` | 锁定 FastRelight 基线代码、commit、环境和数据清单 |
| [-] | `TASK-DD540CF915` | 统一 RGB、LiDAR、相机、实例、光照和编辑操作的数据合约 |
| [-] | `TASK-C0D73ED316` | 复现一个 Waymo 短序列并导出五类组件层 |
| [-] | `TASK-84E68643E9` | 恢复并验收静态背景与天空基线 |
| [-] | `TASK-E5886BDB18` | 恢复并验收刚性车辆基线 |
| [-] | `TASK-9C662E26DC` | 工程化并扩展行人重建原型 |
| [-] | `TASK-C844EE4497` | 建立通用形变体最小基线 |
| [-] | `TASK-C020D3B4C2` | 建立 CARLA 多光照定量基准 |
| [-] | `TASK-C312DFAB8F` | 实现全局环境光重打光基线 |
| [-] | `TASK-BA65540608` | 实现空间光照残差、材质和阴影可见性 |
| [-] | `TASK-31CE2EFAFC` | 完成联合训练、消融、真实数据验证与论文证据冻结 |

状态标记 `[-]` 表示已归档终止或已降级，区别于未开始的 `[ ]`。
