# 双表示互导优化的成立条件：立论与证伪设计

> 对应 `IDEA-4DC79F20C0C6`。本文先给出论点与支撑证据，再设计**能够推翻该论点的实验**，最后才谈方法。
> 顺序如此安排，是因为此前七个候选方向连续被证伪，其中多次源于先设计方法、后才发现问题不成立。

## 1. 论点

> **SDF ↔ 3D Gaussians、2DGS ↔ 3DGS 等双表示互导优化已被多篇工作采用并取得效果，但无人研究该循环的成立条件——何时收敛、何时把一方的误差放大后喂给另一方、warm-up 与切换时机依据什么确定。**

## 2. 支撑证据

### 2.1 语料检索

在 1239 篇会议全文（ICCV 2025 + CVPR 2026 部分）中，限定 3DGS / 隐式表示上下文：

| 子表述 | 主题级论文数 |
|---|---:|
| 互导 / 相互监督 | 2 |
| 交替优化 | 4 |
| 双表示混合 | 9 |
| **收敛性 / 稳定性分析** | **0** |
| 初始化敏感 / 坏极小值 | 1 |

**同时讨论「互导或交替优化」与「收敛或稳定性」的论文：0 篇。**

强信号仅命中 MGSR 自身。即：该模式在被使用，但未被检视。

### 2.2 两篇采用该模式的工作均未分析它

**GS-Octree**（arXiv:2406.18199）结论原文：

> "The method confirms the possibility that Gaussians can guide geometric optimization, and **good geometry** can further optimize Gaussian points."

「good」一词承担了全部重量。若几何本身有误，循环是自我纠正还是互相强化，论文未回答。该文亦无 Limitations 段。

**MGSR**（ICCV 2025）原文：

> "Prior to alternating optimization, the two modules undergo an **independent warm-up stage**, and an **auto-stop strategy** is introduced to reduce unnecessary computational burdens."
> "To the best of our knowledge, MGSR is the **first** GS-based approach that investigates the simultaneous enhancement of rendering and reconstruction, as well as the **first mutual-boosted work** on GS involving both 2DGS and 3DGS."

作者自述为首次，说明该线刚开启，尚无回头检视。warm-up 时长、切换时机、auto-stop 判据均无原理依据。

### 2.3 三个手工旋钮已定位

| 旋钮 | 出处 | 当前依据 |
|---|---|---|
| 八叉树细分层级 | GS-Octree | 正文仅述「according to the data contained in each node」与「progressive refinement guided by the SDF」，Figure 3 展示 level 6–9 的效果，**层级为手工指定的超参**，未给出细分准则 |
| warm-up 时长 / 切换频率 / auto-stop | MGSR | 无原理，工程选择 |
| singular-Hessian 与 Eikonal 的权重 | Wang et al., SIGGRAPH Asia 2023，被 GS-Octree 借用 | **两次代码审计修正**：(1) 实现中并无退火，底层库 `VolumePointsHessEikon` 的默认权重是五项一律 `2e-4`，而 `train-big.py` 传入 `scale_h=1e-12`、`scale_e=1e-6`——把 Hessian 项压到默认值的一亿分之一、比例 1:10⁶，论文与代码均无说明；(2) 该约束**只作用于主循环（SDF 光滑化阶段），final 阶段不使用**。见 `2026-08-24_mgsr_code_audit.md` |

### GS-Octree 侧的误差传播路径（由代码结构确定）

`train-big.py` 分两段：主循环（第 206 行起，SDF 与 Gaussian 互导，Hessian/Eikonal 约束
SDF 的正则性）与 final 阶段（第 531 行起，把 Gaussian 加回来做最终优化，**不再有 Hessian
约束**）。

因此第三个旋钮只在第一段生效，而**第一段的输出正是第二段的输入**。若第一段的 SDF 因权重
设置不当而过度平滑或欠约束，误差将直接传入第二段，且不再有正则项能够纠正。

这是假设 H1 所指的误差传播路径在 GS-Octree 侧的具体形态，比原先的笼统表述更具体、更可实验：
**E-β 的扰动注入点可直接选在两段的交界处。**

### 2.4 借用项的适用边界与原猜测不符

singular-Hessian 的原理是：SDF 的 Hessian 在表面附近的薄壳空间内奇异（微分几何结论），故强制近表面点 Hessian 行列式为零。

**原作者自述的局限是 LiDAR 类输入**——条纹分布、稀疏、大量缺失；他们反而声称在尖锐边（octa-flower）上优于 Hessian energy 与 DiGS。

因此「尖锐/薄结构失效」这一先前猜测**不成立**。真实边界在**点分布不规则、稀疏、缺失**时——而这恰是 GS-Octree 场景中的典型形态：强光下高光区域的 Gaussian 分布本就不规则。

## 3. 可证伪的假设

| # | 假设 | 若为假则 |
|---|---|---|
| **H1** | 存在可构造的初始条件或场景，使互导优化的结果**劣于**单向引导或单一表示 | 不存在失效区域，无问题可研究 |
| **H2** | 三个旋钮的取值对最终几何精度的影响**显著超出随机种子波动** | 当前启发式取值已足够，无改进空间 |
| **H3** | 循环的成败在优化**早期**即可由某个可测信号预示 | 无法预测则只能事后判断，方法价值大减 |

三条中 **H1 是根基**：若 H1 为假，整个论点崩塌，应立即放弃。

## 4. 证伪实验设计

原则：**每个实验的设计目标是推翻对应假设，而非确认它。** 先跑最可能推翻 H1 的实验。

### E-α 循环消融（检验 H1，最高优先级）

- **做什么**：同一批场景上跑四种配置——仅表示 A（八叉树 SDF）、仅表示 B（3DGS）、单向 A→B、双向 A↔B。
- **得到**：几何精度（Chamfer / F-score）与渲染质量（PSNR/SSIM/LPIPS）的四路对比。
- **推翻条件**：若 A↔B 在所有场景上稳健优于 A→B，则 H1 为假。
- **数据**：NeRF-Synthetic、OmniObject3D（GS-Octree 原用数据），单卡可跑。

### E-β 受控误差注入（检验 H1 的机制，核心实验）

- **做什么**：取已收敛的良好 SDF，注入**可控幅度与类型**的扰动（低频形变、局部凹陷、高频噪声、拓扑错误），再运行互导循环，测量扰动随迭代是**收缩还是增长**。
- **得到**：扰动幅度 → 迭代次数的演化曲线，按扰动类型分层。
- **为何是核心**：它直接测量「误差被纠正还是被放大」，且不依赖场景差异这一混杂因素。
- **推翻条件**：若所有扰动类型均单调收缩，H1 为假。

### E-γ 旋钮敏感性（检验 H2）

- **做什么**：扫描三个旋钮（细分层级、warm-up 时长与切换频率、Hessian 退火曲线），同时以 3 个随机种子建立波动基线。
- **得到**：各旋钮的效应量与种子波动的比值。
- **推翻条件**：效应量与种子波动同量级则 H2 为假。

### E-δ 早期可预测性（检验 H3，仅在 H1 成立后进行）

- **做什么**：从 E-α/E-β 的训练轨迹中，寻找在早期迭代即与最终成败相关的可测量（两表示间的一致性残差、SDF 梯度范数分布、Gaussian 离面比例等）。
- **推翻条件**：无任何早期量与最终结果相关。

## 5. 止损点

- **E-α 与 E-β 均未推翻 H1 之前，不进入方法设计。**
- 若 E-α 显示互导稳健更优且 E-β 显示扰动单调收缩 → **论点不成立，放弃该方向**，不再寻找第四个实验来挽救。
- 若 E-γ 显示旋钮效应量与种子波动同量级 → 论点降级为「循环稳健但缺乏理论说明」，价值大减，需重新评估是否值得继续。

## 6. 预注册

在见到结果前固定：

- 几何指标为 Chamfer distance 与 F-score（阈值随数据集惯例），渲染指标为 PSNR/SSIM/LPIPS。
- 探索阶段每配置 1 个随机种子；进入结论的对比一律 3 个种子并报告置信区间。
- H2 的判据为：旋钮效应量的 95% 置信区间与种子波动的 95% 置信区间不重叠。
- 所有训练须经 `gpu-budget` 约束并报告实测占空比。

## 7. 尚未完成的证伪

- **通道 B 引文图谱未走**：需在 Semantic Scholar 上查 GS-Octree、MGSR 与 Neural-Singular-Hessian 的前向引用，确认无人已做该分析。**该项须由研究者执行。**
- 语料仅覆盖 ICCV 2025 与部分 CVPR 2026（1239 篇，仍在增长），未覆盖 SIGGRAPH、TOG、TVCG 等图形学场馆——而双表示混合与几何正则的工作常发表于此，**这是当前最大的检索盲区**。
- 未核实 GS-Octree 与 MGSR 的代码可运行性。

## 8. 与既往工作的关系

本方向不否定 GS-Octree 与 MGSR，而是检视二者共享的基础模式。若结论为「循环在多数条件下稳健」，则是对该模式的正面支持；若发现失效区域，则给出使用条件。两种结果都有价值，但只有后者足以支撑一篇方法论文——**这一点必须在投入前认清，不得在结果出来后重新定义成功标准。**

---

## 9. 重大修正：MGSR 已经报告了这个现象（2026-08-25）

引文图通道（Semantic Scholar 前向引用）→ 全文复核，读到了 MGSR 的完整消融表。
**第 3 节的 H1 需要改写：它不再是一个假设。**

### 9.1 MGSR Table 4 原始数据（OmniObject3D）

| Model | 互导迭代 | 双向 BP | Auto-stop | w2D | w3D | w_depth | SSIM↑ | PSNR↑ | NC↑ | CD↓ |
|---|---|---|---|---|---|---|---|---|---|---|
| A | 20k | No | Yes | 0 | 1 | 0 | 0.9755 | 35.10 | – | – |
| B | 20k | No | Yes | 1 | 0 | 0 | – | – | 90.16 | 0.92 |
| C | 20k | No | Yes | 0.3 | 0.7 | 0.01 | **0.9861** | **37.88** | 90.54 | 0.92 |
| D | 20k | No | Yes | 0.7 | 0.3 | 0.01 | 0.9825 | 36.99 | **90.62** | 0.92 |
| E/I/M（完整） | 20k | No | Yes | 0.5 | 0.5 | 0.01 | 0.9850 | 37.69 | 90.60 | **0.90** |
| F | 20k | No | Yes | 0.5 | 0.5 | 0.1 | 0.9803 | 36.42 | 90.60 | 0.91 |
| G | 5k | No | Yes | 0.5 | 0.5 | 0.01 | 0.9820 | 36.76 | 90.40 | 0.89 |
| H | 10k | No | Yes | 0.5 | 0.5 | 0.01 | 0.9842 | 37.37 | 90.50 | 0.91 |
| J | 30k | No | Yes | 0.5 | 0.5 | 0.01 | 0.9850 | 37.71 | 90.60 | 0.91 |
| **K** | 20k | **Yes** | Yes | 0.5 | 0.5 | 0.01 | 0.9813 | 36.64 | 90.57 | 0.91 |
| L | 20k | No | **No** | 0.5 | 0.5 | 0.01 | 0.9831 | 37.01 | 90.56 | 0.92 |

### 9.2 三个读出来的事实

**（1）双向梯度是有害的，MGSR 自己测出来了。**

K（双向 BP）对 M（单向 BP）全面变差：PSNR 36.64 vs 37.69，SSIM 0.9813 vs 0.9850，
NC 90.57 vs 90.60，CD 0.91 vs 0.90。

MGSR 的解释只有一句：

> "It is mainly due to the bidirectional BP influences optimization of the branch
> which provides supervision."

**即：让梯度双向流动会破坏提供监督的那一支，所以他们用 stop-gradient 把环切断了。**
这正是我们假设的反馈环不稳定性——**但它已经是被观测到的事实，不是待证的假设**。
MGSR 观测到了、用一句话命名了、用一个全局开关绕过了，**没有刻画它何时发生、为何发生**。

**这对我们是好消息**：现象的存在性不需要我们证明（导师的论文已经证了），
我们要做的是它缺的那一半——**条件刻画**。风险从"现象可能不存在"降为"条件可能不可刻画"。

**（2）互导对重建的增益远小于对渲染的增益。**

以 A/B（无互导）对 M（完整）：

| 指标 | 无互导 | 完整 | 增益 |
|---|---|---|---|
| PSNR（渲染） | 35.10 | 37.69 | **+2.59 dB** |
| NC（几何，×10²） | 90.16 | 90.60 | +0.44 → 实为 +0.0044 |
| CD（几何，×10³） | 0.92 | 0.90 | −0.02 → 实为 −0.00002 |

**互导这套机制主要在提升渲染，对几何几乎没动。**
导师要求做重建方向——**重建侧恰好就是这套机制留下空间最大的地方**。
这条比任何"空位论证"都实在：不是没人做，是做了但那一侧没做出来。

**（3）互导迭代在 20k 处饱和。**

G→H→I→J（5k/10k/20k/30k）：PSNR 36.76 → 37.37 → 37.69 → 37.71，
NC 90.40 → 90.50 → 90.60 → 90.60。20k 之后完全不动。

MGSR 的处理是把 20k 定为超参。但**饱和本身是个信号**：
要么到了不动点（两支互相一致，不再产生新信息），要么增益被某种误差累积抵消。
这两种情况的区分方式完全不同，而没人区分过。这正好是 E-δ 的抓手。

### 9.3 一处需要向作者确认的文本不一致

正文与表格/表题对不上：

- 正文："Auto-stop warm-up. Experimental results (**Models G-J**) show that the auto-stop
  strategy…" —— 但表中 G–J 的 Auto-stop 全是 Yes，变化的是迭代数；表题也写
  "iterations of mutual-boosted optimization (Models G-J)"。
- 正文："Mutual-boosted iterations. …exceed 20k… (**Models L and M**)" —— 但 L 与 M
  差的是 Auto-stop，不是迭代数。

即这两段的模型编号疑似互换。K vs M（双向 BP）那段是对的。
**可能是 PDF 抽取问题，也可能是原文笔误，需要向作者核对**——
在我们引用这些数字之前必须确认。

### 9.4 论点改写

原论点（第 1 节）："双表示互导优化的成立条件无人研究。"
证据变化后应改为：

> **互导优化的失效已被观测并被 hack 绕过（MGSR 的 stop-gradient、20k 截断），
> 但无人刻画其成立条件。且现有方法的增益集中在渲染侧，重建侧的增益接近于零。**

这个表述更强：它有导师论文的实测数据背书，且直接落在重建方向上。

### 9.5 对实验设计的影响

- **E-α（循环消融）降级。** MGSR 的 Model A/B/K/M 已覆盖"有无互导""单向/双向"。
  我们重跑只作为**基线复现的自检**，不再是贡献。
- **E-β（受控误差注入）升为最高优先级。** MGSR 说双向 BP "influences optimization of
  the branch which provides supervision"——这句话没有任何量化。E-β 正是量化它的实验。
- **新增 E-ε：分层增益分解。** 把 A/B vs M 的增益按场景/区域属性分层，
  定位"几何增益为何接近零"。若能找到某类区域几何增益显著为负，
  那就是全局 stop-gradient 换成条件耦合的直接依据。
- **止损点更新**：若 E-β 显示误差注入后两支的退化幅度无法用任何可观测量预测，
  则条件不可刻画，方向作废。

### 9.6 本次检索的口径

- 引文图种子：GS-Octree（被引 14，全量）、Neural-Singular-Hessian（63，全量）、
  2DGS（1000，**触顶截断**）、SuGaR（915，**触顶截断**）。去重后 1674 篇。
- 关键交叉 6 篇，同时引用 ≥2 个种子 314 篇。
- 五类探针（收敛性论述 / 失败条件 / 误差传播 / 消融互导环 / 初始化依赖）
  在已取得全文的 4 篇上：**收敛性论述全部为 0**。核心论点在这一路上仍然成立。
- GS-ROR2（TOG 2024）提供一条可引用的旁证：
  "GSDF … **does not converge** for reflective objects even with mask loss"
  —— 又一个互导系统在某类场景上不收敛，同样只是顺带一提，无分析。
- 未解析：GSDF、GSurf、两篇 survey、SplatlessDF、Gaussian-Voxel Duet
  （Semantic Scholar 检索未命中，需换检索式重试）。**这些尚未排除。**

---

## 10. Survey 通道核验（2026-08-25）

综述是检验「无人研究 X」这类断言最省力的地方：若某条线上确有人做过条件分析，
专门的综述几乎不可能漏掉。引文图找出两篇引用了 GS-Octree 的表面重建综述。

### 10.1 PeerJ CS 2025（已取得全文，自行量测）

《A survey on surface reconstruction based on 3D Gaussian splatting》，
PeerJ Computer Science, 2025, DOI 10.7717/peerj-cs.3034。
经 Europe PMC 取得全文 XML（216 KB，正文 90269 字符），用与 position paper
一致的探针词自行量测：

| 探针 | 命中 |
|---|---|
| 收敛性论述 | **0** |
| 失败条件 | **0** |
| 误差传播 | **0** |
| 稳定性/发散 | 2 |
| 互导措辞 | 3 |

仅有的 2 处「稳定性」命中都在讲别的事：一处是高光区的深度-法向一致性项，
一处是 Poisson 重建在镜面区不稳定。3 处「互导措辞」全是编目式的 "jointly optimize"，
没有任何分析。

该综述点名的双表示方法及出现次数：
GSDF 5、NeuSG 4、3DGSR 4、**GS-Octree / Octree-GS 共 4**、SuGaR 2、2DGS 6；
MGSR 0、GS-ROR 0（两者发表晚于该综述）。

**即：一篇专门综述表面重建的文章，把双表示耦合方法逐一编目，
却对它们的收敛性、稳定性、误差传播只字未提。** 这是对核心论点最直接的支持。

（另经 WebFetch 读该综述的 Challenges 节，答复为三项——数据集质量、大场景批量提取、
几何细节质量——均不涉及优化可靠性。此项为模型摘读，非本人量测，强度低于上表。）

### 10.2 Expert Systems with Applications 2026（**未能核验**）

《3D Gaussian splatting for reconstruction: Methods, datasets, and comparisons》，
Expert Systems with Applications, 2026。Elsevier 付费墙，全文取不到。

**此项标记为未核验，不计入支持证据。** 若日后能通过学校订阅取得，应优先补验——
它是目前唯一一篇可能推翻 10.1 结论的综述。

### 10.3 对研究者本人的一条事实

两篇综述均引用 GS-Octree。在 10.1 中它与 GSDF、NeuSG、3DGSR 同级被列为
双表示耦合的代表方法。谈合作时这比「我做过一篇相关工作」有力得多。

---

## 11. 第二次修正：两篇工作已经解释了一半（2026-08-25）

引文图第二层（1674 篇引用者 → 189 篇命中筛选 → 44 篇全文）点名了两篇必须正视的工作。
读完之后**论点要再收窄一次**，但收窄后更具体、更可做。

### 11.1 CarGS：目标冲突的机制已被解释

《Evolving High-Quality Rendering and Reconstruction in a Unified Framework with
Contribution Reassignment》(arXiv:2503.00881, 2025)

它对「几何正则为何损害渲染」给出了**机制性解释**。原文（§3.2）：

> 考虑一条光线穿过三个高斯 G1, G2, G3，其中 G2 靠近真实表面、G1/G3 远离。
> 颜色与深度都由贡献权重 w_i 加权而成。
> **只加渲染正则时，w1 与 w3 倾向增大**（以捕捉更丰富的纹理）；
> **加上几何正则后，w1 与 w3 因 z1/z3 偏离 SDF 零水平集而倾向减小**，w2 增大以提供
> 更一致的深度。**这种贡献权重的此消彼长构成冲突**，导致加几何正则后渲染性能下降。

**这正是 §9.2 那组数字的机制。** MGSR 渲染 +2.59 dB、几何近零，不是巧合——
两个目标对同一组 w_i 的要求相反。

CarGS 还明确把自己与「dual-model approaches (e.g., GSDF)」对立，主张用统一网络
而非两个耦合模型。**这是一个正面竞争的框架，必须引用并回应。**

**但它分析的是单个 3DGS 模型内部、两个损失之间的冲突，
不是两个表示之间交换监督的环路动力学。** 这是我们与它的分界线。

### 11.2 GEAR：交替优化的失效已被实证刻画（在另一个领域）

《GEAR: GEometry-motion Alternating Refinement for Articulated Object Modeling》
(arXiv:2604.07728, 2026)

它给出三分类法与训练动力学曲线证据：

| 方案 | GEAR 报告的行为 |
|---|---|
| 联合（同时优化） | 「**oscillates heavily without converging**」，归因于**误差归属歧义**（error attribution ambiguity）——优化器无法判断是哪一支错了，于是扭曲几何去补偿错误的运动估计，落入非物理局部极小 |
| 分阶段（单向） | 「descends initially but **stagnates**, as its **unidirectional pipeline allows early segmentation errors to irreversibly corrupt** motion estimation」 |
| 交替（EM） | 「step-like」误差下降，稳定收敛 |

**「误差归属歧义」这个命名值得直接借用**：它精确描述了 MGSR 用 stop-gradient
绕过的东西——双向 BP 时，监督支被反向影响，正是因为优化器无法归属误差。
MGSR 那句 "bidirectional BP influences optimization of the branch which provides
supervision" 与 GEAR 的诊断是同一件事，只是 MGSR 没有命名也没有测量。

**但 GEAR 的两个耦合对象是「几何与运动」，不是同一几何的两种表示；
且它论证的是「交替更好」，不是「交替何时也会失败」。**

GEAR 的方法学可直接借用：**跟踪 joint / staged / alternating 三条误差曲线**
本身就是 E-α 与 E-δ 该用的形式。这比我原设计的「四种配置比终值」信息量大得多。

### 11.3 一条反面证据：环路被断言为自纠正，无人验证

《Radiometrically Consistent Gaussian Surfels for Inverse Rendering》(ICLR 2026)
反复使用 "**self-correcting feedback loop**" 描述其残差最小化机制，
但全文无任何收敛性或稳定性论证——**这正是我们要针对的那类未经检验的断言**。

### 11.4 论点第三版

前两版分别被 MGSR 的消融（§9）与本节收窄。现表述为：

> 双表示互导中，**耦合的强度与方向**是未经研究的自由度。
> 目标冲突的机制已被解释（CarGS，单模型内），
> 交替与联合的差异已被实证刻画（GEAR，几何-运动），
> 但**没有任何工作研究过：当两条耦合通道强度相差 50 倍时（MGSR 的 0.5 : 0.01），
> 增益为何全部流向强通道那一侧，以及这个比值是否可调。**

这一版的好处：它不再依赖「无人研究」这种易被推翻的全称断言，
而是指向一个**具体的、可测量的、代码里就摆着的**自由度。

### 11.5 由此产生的核心实验（取代原 E-α 的最高优先级）

**E-ζ：耦合强度的帕累托前沿。**

MGSR 的 `total_loss = 0.5*loss_ref + 0.01*depth_loss + 0.5*loss_n`。
扫描 geo→ref 的权重（0.01 的那一项）与 ref→geo 的 `w2`，
在「渲染质量 × 几何精度」平面上画出前沿。

三种可能结果，**每一种都是可发表的结论**：

1. **存在真实权衡** → MGSR 的取值只是前沿上偏向渲染的一点，
   「互导提升重建」这一说法需要限定。这直接呼应 CarGS 的贡献冲突机制。
2. **前沿在几何方向平坦**（加大 geo→ref 权重也不涨几何）→
   限制几何的不是耦合强度而是别的东西（表示能力？监督信号质量？），
   那就要去找那个东西——这是更深的问题。
3. **存在更优点**（某个权重同时优于 MGSR 的取值）→ 直接的方法改进。

**为何这个实验优于原 E-α**：E-α 问「互导有没有用」，MGSR 已答；
E-ζ 问「耦合强度该取多少」，无人问过，且答案必然落在上述三种之一。
**没有「什么都没发现」的分支**——这是止损设计上的关键改进。

配套仍保留 **E-β（受控误差注入）**，且其首个注入类型已由代码审计确定为
「向 depth 的 max 附近注入离群值」（见 code audit §互导环结构，
`d / d.max()` 对单像素离群值敏感）。

### 11.6 本轮口径

- 引文图 1674 篇 → 带 arXiv id 的 1150 篇 → 命中筛选 189 篇 → **本轮全文 44 篇**。
  **余下 145 篇尚未排除**，下载进行中。
- 44 篇中五类探针全为 0 的 20 篇；「收敛性论述 > 0」的 9 篇，
  逐条查看后仅 GEAR 一篇是真的在论证优化行为，其余为
  "converges to a competitive level" 之类的顺带用词。
- **失败条件探针在 44 篇中总计仅 2 次命中**（GS-ROR2、TRiGS），均为顺带一提。

---

## 12. E-ζ 首批结果：加强引导反而损害被引导支（2026-08-25 02:20）

### 12.1 数据

DTU scan24，复用同一组 geo/ref 检查点，仅互导阶段 20k 步。
几何度量为 DTU 官方协议（0.2 mm 体素下采样 + ObsMask + 地面剔除 + 20 mm 截断），
真值 `stl024_total.ply`。

| 分支 | 量 | w_depth=0.01（原值） | w_depth=0.5（50×） | 变化 |
|---|---|---|---|---|
| geo（2DGS，重建输出） | overall | 0.9161 | 0.9077 | −0.9% |
| | accuracy | 0.8344 | 0.8084 | −3.1% |
| | completeness | 0.9978 | 1.0069 | +0.9% |
| **ref（3DGS）** | **overall** | **1.2339** | **1.3155** | **+6.6%（变差）** |
| | accuracy | 1.6840 | 1.8141 | +7.7%（变差） |
| | completeness | 0.7838 | 0.8170 | +4.2%（变差） |

训练轨迹（`ezeta_trace.tsv`，前 20 点均值 → 后 20 点均值）在 w=0.5 下：
`loss_ref` −22.3%、`depth_loss` −30.8%、`loss_n` **+13.7%**。

### 12.2 读法

**（1）geo→ref 的强度对 geo 支几乎无影响。**

`depth_loss = l2(out_depth_ref, surf_depth.detach())`——`surf_depth` 已 detach，
梯度**只流向 ref 支**。因此 `w_depth` 对 geo 支的作用只能是经环路的二阶效应。
50 倍变化换来 <1% 的 Chamfer 变化，**说明这个二阶效应可以忽略**。

即：**这个「互导」环在几何结果上是单向的。**

**（2）加强引导，损害的是被引导的那一支。**

ref 支 Chamfer 从 1.2339 劣化到 1.3155。这是被 `depth_loss` 直接作用的支，
理应被几何引导改善，实测相反。

**（3）机制的头号嫌疑已由代码审计预先标出。**

```python
out_depth_ref    = out_depth_ref    / out_depth_ref.max()
surf_depth_fixed = surf_depth_fixed / surf_depth_fixed.max()
depth_loss = l2_loss(out_depth_ref, surf_depth_fixed)
```

**两侧各按自身 max 归一化，比较的不是同一个量。** 任一支出现一个离群深度，
整幅图尺度即被改变，L2 便在强迫 ref 支去拟合一个被错误缩放的目标。
`w=0.01` 时损害小到淹没在噪声里，`w=0.5` 时显形。

**这个嫌疑是在看到结果之前、由代码审计独立提出的**（见 code audit
「互导环的完整结构」§三处值得追问的设计 之（2）），非事后编造。

### 12.3 若机制成立，贡献的性质改变

不是「发现互导脆弱」（现象 MGSR 已报告），
而是「**给出原因并给出修法**」：把 max 归一化换成稳健尺度对齐
（中位数比值，或最小二乘 scale+shift），使耦合可以被安全加强。

对应实验 **E-β′**：固定 `w_depth=0.5`，只替换归一化方式，比较 ref 支 Chamfer。
- 若稳健对齐下 ref 支不再劣化 → 机制确认，且修法有效。
- 若仍劣化 → 机制被证伪，另找原因（此时 12.2(2) 仍是有效发现）。

### 12.4 必须声明的局限

1. **单场景、单种子。** scan24 一个场景，无重复。差异 6.6% 是否超出种子波动**未知**——
   这是当前最大的不确定性，必须补种子重复实验。
2. **用的是高斯中心，不是 TSDF 网格。** 因此 **0.9161 这类绝对值不可与论文比较**；
   仅同一流程内部可比。
3. 只扫了 `w_depth` 一个方向。`w2`（ref→geo，即直接作用于几何的通道）尚未扫描。
4. 第三点 `w_depth=0.001` 尚在运行。**若其 ref Chamfer 不优于基线，则单调性不成立，
   上述读法需重估。**

---

## 13. 引文图第二层收官：188 篇全文（2026-08-25 03:00）

189 篇候选中带 arXiv id 且可取得全文的 **188 篇**全部处理完毕。

| 口径 | 数 |
|---|---|
| 全文取得 | 188 |
| 五类探针**全为 0** | 104 |
| 收敛性论述 > 0 | 38 |

38 篇中逐条查看后，**无一篇分析双表示互导的成立条件**。绝大多数是
"converges to a competitive level" 之类的顺带用词。真正在论证优化行为的仅下列四篇，
**每一篇都恰好差一步**：

| 论文 | 它做了什么 | 与我们的距离 |
|---|---|---|
| **GEAR** (2604.07728) | 联合 / 分阶段 / 交替三者的训练动力学对比，命名「误差归属歧义」 | 耦合对象是**几何与运动**，非同一几何的两种表示 |
| **CarGS** (2503.00881) | 几何正则与渲染的**贡献权重冲突**机制 | **单模型内两个损失**的冲突，非两表示交换监督 |
| **MooMIns** (2606.14389) | 扫描位姿噪声幅度，定位方法**不再收敛的阈值**（150 mm） | 扰动的是**位姿**，非表示间的监督信号 |
| **Beyond Heuristics** (2605.00408) | 密度控制的收敛性证明（TRPO 单调改进下界） | 对象是**密度控制策略**，非表示耦合 |

### 两条对我们有用的结论

**（1）文献位置成立。** 四篇最接近的工作分别在耦合对象、作用层次、扰动量、
分析对象上与我们不同。「双表示互导的耦合强度与方向未被研究」这一表述可以站住。

**（2）MooMIns 给了 E-β 的方法学先例。** 它的做法——扫描扰动幅度直到方法失效，
报告那个阈值——正是我们要对 depth 引导做的事。有先例意味着这种实验形式是被接受的，
不必自己论证其正当性。

**（3）Beyond Heuristics 说明这个圈子接受收敛性分析。** 3DGS 领域里带
"Proof of Optimality and Convergence" 的论文能发，降低了「太理论、不像 CV 论文」的风险。

### 口径与残余缺口

- 引文图种子中 2DGS(1000)、SuGaR(915) **触顶截断**，非全量；GS-Octree(14)、NSH(63) 为全量。
- 1674 篇引用者中 **524 篇无 arXiv id**，本路取不到全文，是固有缺口。
- 两篇 survey 中 PeerJ CS 2025 已全文核验（§10.1），
  Expert Systems with Applications 2026 因付费墙**未能核验**。

---

## 14. 种子重复：当前最大不确定性（2026-08-25 03:05）

§12 的三个权重点**共用 seed=0**——`safe_state` 里 `random.seed(0)` 是写死的，
而互导阶段的视角选取用的正是 `random.randint`。

因此：
- **好的一面**：三点之间视角顺序完全一致，权重比较不受采样顺序干扰。
- **坏的一面**：**完全没有 run-to-run 波动的估计**。§12 那句「6.6% 的劣化」
  目前**没有证据**说明它超出噪声。这是当前唯一能推翻结论的因素。

已将种子改为可配置（`MGSR_SEED`，默认仍为 0，不改变原有行为），
并排队 seed ∈ {1, 2} × w_depth ∈ {0.01, 0.5} 四个跑（`tools/run_seeds.sh`）。

**判据（先写下来，避免事后找解释）：**

- 若两个新种子下 w=0.5 均劣于 w=0.01，且幅度与 seed=0 的 6.6% 同量级
  → 效应真实，§12 的读法成立。
- 若**同一权重**在不同种子间的散布已达 5–6%
  → **§12 结论作废**，需以更多种子重做，或改用对种子不敏感的度量。
- 若方向不一致（某个种子下 w=0.5 反而更好）
  → 效应不稳定，至少需要 5 个种子才能谈。

---

## 15. 修正 §12：第四个点推翻了单调解释（2026-08-25 03:25）

§12 写于只有三个点时，当时倾向「加强耦合单调劣化被引导支」。
第四个点 `w_depth=0.05` 跑完后，**这个解释不成立**。

### 15.1 完整四点

| w_depth | geo 支 overall | ref 支 overall |
|---|---|---|
| 0.001 | 0.9218 | 1.2381 |
| **0.01（MGSR 原值）** | 0.9161 | 1.2339 |
| **0.05** | 0.9242 | **1.1919**（最好，比原值好 3.4%） |
| 0.5 | **0.9077**（最好） | 1.3155（最差，比原值差 6.6%） |

- **ref 支非单调**：在 0.05 处有内部最优，之后才劣化。
- **geo 支也非单调**，且跨度仅 1.8%（0.9077–0.9242），无明显趋势。

### 15.2 读法的改变

原读法「加强引导损害被引导支」→ 应改为：

> `w_depth` 与 ref 支几何之间存在**内部最优**。MGSR 取的 0.01 不是最优，
> 0.05 更好；但超过某个点（≤0.5）后急剧劣化。

这落在 E-ζ 预设的**第三种结局「存在更优点」**上，而非第一种「单调权衡」。

### 15.3 但噪声地板可能吞掉一切

geo 支在四个权重下非单调地散布 1.8%，**这本身就是噪声地板的一个上界估计**。
若 run-to-run 波动就在 1–2%：

- ref 的 3.4% 改善 → **勉强**，需重复才敢说；
- ref 的 6.6% 劣化 → 大概率真实，但仍需重复。

§14 排队的种子重复因此从「补充验证」升级为**判定性实验**。判据不变，
且**在看到这四点之后不做任何修改**——判据写于 §14，早于本节。

### 15.4 一处方法学自省

§12 是在三点上写的，四点一到就要改。教训：
**在噪声地板未知时，任何趋势解释都应推迟到重复实验之后。**
本次的补救是及时记录并公开修正，而非等到最后一并呈现。

下一步顺序不变：种子重复 → 若效应成立再做 E-β′（替换 max 归一化验证机制）。

---

## 16. 种子重复的判定结果：效应真实，噪声地板 0.36%（2026-08-25 06:30）

按 §14 预先写下的判据对号。**判据未在看到数据后修改。**

### 16.1 数据（DTU scan24，n=3 种子）

**ref 支 overall（受 `depth_loss` 直接作用的一支）**

| seed | w=0.01（原值） | w=0.5 |
|---|---|---|
| 0 | 1.2339 | 1.3155 |
| 1 | 1.2367 | 1.2960 |
| 2 | 1.2383 | 1.2978 |
| **均值** | **1.2363** | **1.3031** |
| 标准差 | 0.0022 | 0.0108 |
| 极差 | 0.0044（**0.36%**） | 0.0195（1.5%） |

**geo 支 overall（重建输出）**

| seed | w=0.01 | w=0.5 |
|---|---|---|
| 0 | 0.9161 | 0.9077 |
| 1 | 0.9175 | 0.9037 |
| 2 | 0.9108 | 0.9081 |
| **均值** | **0.9148** | **0.9065** |
| 标准差 | 0.0035 | 0.0024 |

### 16.2 对号

判据一（两种子下 w=0.5 均劣且幅度同量级）→ **命中**。
三个种子全部劣化，幅度 +6.6% / +4.8% / +4.8%，均值 **+5.4%**。

判据二（同权重跨种子散布达 5–6% 则结论作废）→ **未命中**。
实测散布 **0.36%**，比我在 §15.3 从权重扫描估计的 1–2% **小一个量级**。

判据三（方向不一致）→ **未命中**。三个种子方向一致。

**结论：w=0.5 使 ref 支几何劣化 5.4%，是真实效应，约 30 个标准差。**

### 16.3 一个此前未注意到的事实

geo 支在 w=0.5 下**改善 0.91%**（0.9148 → 0.9065），三个种子全部一致，
两组区间不重叠（w=0.01 最好 0.9108 vs w=0.5 最差 0.9081）。

**两支方向相反**：加强 geo→ref 耦合 → geo 支变好、ref 支变差。
这不是「引导失效」，而是**两支之间的真实此消彼长**。

注意这与 §12.2(1) 的判断需要修正：当时说「geo 支变化 <1% 属噪声量级」，
现在噪声地板确定为 0.36%，那 0.91% 就**不是噪声**，而是可测的真实改善。
§12 的那句读法作废。

### 16.4 内部最优点：seed 0 下 −3.6%，20 个标准差，但 n=1

`w=0.05` 在 seed 0 下得 ref overall **1.1919**，比基线均值 1.2363 低 **3.6%**，
以 0.0022 的标准差衡量约 20 σ。

**但只有一个种子。** 已排 seed 1、2 的复现，另补 w=0.1（seed 0）判断
0.05 与 0.5 之间曲线如何转向（`tools/run_optimum.sh`）。

**预注册判据（写于看到结果之前）：**
- 若 w=0.05 在三个种子上均优于 w=0.01 且幅度 ≥2% → 内部最优点成立，
  即 **MGSR 的 0.01 不是最优取值**，这是可直接写入论文的结论。
- 若三个种子中出现劣于 w=0.01 者 → 最优点不成立，seed 0 的 1.1919 属异常，
  需查该次运行是否有异常（点数、发散等）。

### 16.5 当前证据链的状态

| 命题 | 证据强度 |
|---|---|
| 噪声地板 0.36%（ref）/ 0.38%（geo），n=3 | **已确立** |
| w=0.5 使 ref 支劣化 5.4% | **已确立**，n=3，方向一致 |
| w=0.5 使 geo 支改善 0.91% | **已确立**，n=3，区间不重叠 |
| w=0.05 处存在内部最优（−3.6%） | **n=1，待确认** |
| 劣化机制是 max 归一化 | **未验证**，E-β′ 待做 |

仍未解除的局限：单场景（scan24）、高斯中心而非 TSDF 网格
（**绝对值不可与论文比较**）、`w2`（ref→geo 通道）尚未扫描。
